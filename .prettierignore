# Generated / vendored
node_modules/
.git/
.venv/
letsencrypt/
secrets/
data/

# Licenses and other long-form legal text shouldn't be reformatted
LICENSE

# Prettier's YAML parser doesn't love some of eRPC / Traefik's shapes; keep
# these formatted by yamllint + hand only, to preserve the careful alignment
# of comments that the "why" docs reference by line.
config/erpc/erpc.yaml
config/traefik/traefik.yml
config/traefik/dynamic.yml

# Architecture diagram source (Mermaid) — prettier's mermaid support mangles it.
docs/diagrams/*.mmd
