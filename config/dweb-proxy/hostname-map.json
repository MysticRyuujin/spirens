{
  "_comment": "Maps incoming hostnames to the TLD the dweb-proxy should resolve. Source for ${LIMO_HOSTNAME_SUBSTITUTION_CONFIG}; encode via `spirens encode-hostname-map` and pass to the container.",
  "${DWEB_ETH_HOST}": "eth"
}
